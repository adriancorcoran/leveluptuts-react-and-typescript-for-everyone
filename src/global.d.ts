// for libraries without types, you can either

// 1. declare the module as below
// or
// 2. instell the necessary @types library, in this instance `yarn add --dev @types/styled-components`

// declare module "styled-components";
